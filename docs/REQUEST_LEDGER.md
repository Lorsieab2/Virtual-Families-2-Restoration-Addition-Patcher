# VF2 Restoration + Addition Patcher Request Ledger

Last reconciled: 2026-08-30
Published baseline: B153 (`2963ba2`, release
`B153-restoration-addition-patcher`)

This is the durable completeness gate for future builds. A request does not
disappear because it was made in an older chat. A label, checkbox, design note,
dormant hook, or successful link is not proof that a feature is finished.

## Status meanings

- **Shipped / automated-verified**: in the published baseline with
  source/link/package checks.
- **Shipped / in-game QA pending**: shipped, but an important runtime scenario
  still needs a game test.
- **Partial**: implementation exists, but the complete request is not delivered
  or proven.
- **Needs source audit**: prior work may exist but has not been traced from
  setting through native behavior, persistence, packaging, and QA.
- **Not started**: no complete implementation is claimed.
- **Blocked on owned asset/native evidence**: waiting for a self-contained
  source asset or reliable native contract. Outside paths are never a release
  dependency.

## Release invariants

- GitHub is the source of truth and new builds use the newest published build.
- The project and release payload are self-contained.
- Optional features are absent when their setting is off.
- Base-game autonomous behavior choices and likelihoods remain unchanged.
  New routes are additive and explicitly gated.
- Reversible purchases clear their active/save flag and can be bought again.
- Goal progress persists unless a documented action deliberately resets it.
  The Collector's **Sell it all!** choice is the explicit exception: it pays by
  rarity and resets all collections and related goal progress. **Keep** is inert.
- Experimental features stay experimental until critical save/load,
  enable/disable, and in-game routes are proven crash-free.
- Every release updates notes, TODO/changelog, technical discoveries where
  applicable, and the Transparency Log.

## Published B153 baseline

| Request | Status | Evidence / remaining gate |
|---|---|---|
| Holiday Ornaments: 12 mobile IDs, rarity, spawns, sixth page, 72 total, persistence, pickup/drop, Collector integration | Shipped / automated-verified | Linked validators and 16-state matrix; user confirmed spawns. Exact mobile/PC Lucky Rock math is proven and both `Update` and `Add` are byte-locked: spawn attempts double from 3/6600 to 3/3300 and rarity changes from 83/13/4 to 66/26/8. Full save/load and live frequency QA remain. |
| Upright Ornament art and corrected lower-right Candy Cane | Shipped / automated-verified | Canonical sources copied into payload and hash-validated. |
| Page label `Ornaments`, footer strings, Ornamentologist after Bottlologist | Shipped / automated-verified | B152 string/order validator. |
| Holiday Furniture achievement runtime gate | Shipped / in-game QA pending | Exact-SHA `.vf2goal` gate; purchase, award, persistence, and off-state QA remain. |
| Allow Older Pregnancies | Partial / automated-verified | Default-off `.vf2preg` hook ships. Stock under 50; 10% at 50, decreasing to 0.1% floor at 69+; native multiples preserved. All 16 linked layouts, exact-SHA records, helper ABI, stock fallback, and toggle cycles pass; conception/tutorial/birth/save-load QA remains. |
| Reset Ants; Reset/Complete all collections | Shipped / in-game QA pending | Native/package checks exist; verify awards, reset semantics, Holiday on/off, and save/load. |
| 2x/5x/100x Prices and Reset Price Multiplier | Shipped / automated-verified | Both ordinary and career `CalcPrice` returns are hooked; 2x/5x/100x are mutually exclusive and saturate at `INT_MAX`. Reset removes all multiplier inventory flags and returns the current canonical incoming price unchanged, so it does not restore stale cached prices. Broad in-game purchase QA remains. |
| Trigger/Fix malfunctions, Router state, dryer fire, north leaks | Shipped / in-game QA pending | Props and renovation gates linked; gameplay/repair/Handyman/Water Surge QA remains. |
| Rebuy Maid/Gardener to fire and Rockhound/Anti-Spam to remove | Superseded | Maid/Gardener remain Cheat Upgrades-gated rebuy-to-fire rows. Rockhound/Anti-Spam no longer have any repurchase/removal route on the Flea Market rows themselves -- those rows are base game, and Cheat Upgrades `0x158`/`0x159` own the flags instead. |
| B150 behavior variations and gates | B150 source audit complete; linked validation complete; live QA pending | `docs/discoveries.md` records the recovered raw-age, gender, career, nursing, weather, sink, autonomous, variation, and praise-cache rules. Behavior Patches owns the guarded route, and the 16-state executable matrix validates its independent gate and off-state. Live frequency, age/gender/relationship, weather, nursing, manual, and save/reload checks remain. |
| Six-child opposite-sex private romantic time | B158 source implementation; linked static validation pending; live QA pending | Behavior Patches only: an exact current-generation opposite-sex adult spouse pair whose native FamilyTree count at `+0x1B4` is at least 6 routes to native `HandleDropOnVillager +0x26E`, bypasses the refusal/argument branch, and returns from `TryToMakeBaby` before pregnancy. Behavior Patches off preserves the stock route. Live action, no-pregnancy, no-argument, under-six, and patch-off QA remains. |
| Compatibility/creator GUI messages | Shipped / automated-verified | Exact constants exist in exporter and GUI and are covered by GUI/exporter tests. |
| Brokerage Account 11% message | Shipped / in-game QA pending | Exact native store description exists; verify its visible wrapping/layout in game. |
| Mobile Special Upgrade mechanics and persistence | Source-complete / linked QA passed | Brokerage `+2%` and 11% cap, Food Club immediate/daily 500-food delivery and 16-byte save state, and Health Plan quarter-price medicine route are source-proven. Desktop Health Plan ownership now survives reload through hidden achievement record `0xA8+0x08`, separate from the Taters/pregnancy/generation mask at `+0x04`. A fresh cheat-enabled linked diagnostic build contains all late IDs `0x12E-0x13B`, the purchase/effect/save route, the two-adult Marriage Email refusal, and no phantom weather row. Every late row now has a distinct descriptor and semantically distinct source PNG rather than reusing the Reset Achievements trophy; the offline exporter emits those assets and their checkboxes only with the matching Cheat Upgrades executable overlay. Live buy/reload/remove and visual QA remains. |
| No AI Icons | Source/exporter implementation complete; package/runtime QA pending | Default-off optional `No AI Icons` requires Cheat Upgrades and layers 14 hash-checked 90x90 replacement PNGs over only the late Special Upgrade icon targets. Sources are self-contained under `patcher_assets/optional_patches/no_ai_icons`; the description identifies other LDW games, online art sources, or custom-made artwork. Disabling restores the current Cheat Upgrades icons, while disabling Cheat Upgrades removes the late icon layer. |

## B153 priority

| Request | Status | Completion contract |
|---|---|---|
| Fully working Allow Older Pregnancies | Partial / linked validation complete | All 16 layouts now also prove the age-50+ failed-attempt cooldown bypass: the patch skips only the stock `theGameState+0x25AE0` deadline write when either parent is 50+, while flag-off and both-under-50 retain it. Live conception/birth/save-load QA remains. |
| Older Villagers mortality curve | B155.5 source + analytical/simulation + 16-layout linked validation complete | Default-off `.vf2mort` replaces only the birthday old-age roll. It retains threshold `55 + active food groups` (0-4), uses monotonic intensity `0.00365*n + 0.06*max(0,n-55)`, one million-way roll, and a 999999/1000000 cap with no hard maximum age. Full-game calibration uses 60 adults; age 110 takes multiple games and age 122 remains exceptional. Live aging/save/time-away QA remains. |
| Next Generation button around age 60 with age patch | B156 source + 16-layout linked validation complete; live QA pending | When Allow Older Pregnancies is enabled, all four native `CanStartNextGeneration` queries additionally allow the stock Next Generation flow once the oldest active living non-departed villager reaches displayed age 60. Native eligibility is checked first and a surviving child is still required. Native `StartNextGeneration` and its 30-record `MakeRoomInTree` rollover remain unchanged. With `.vf2preg` off, the wrapper returns the native result unchanged. Live age-59/60 Family Tree, transition, no-child, generation-30 rollover, save/reload, and patch-off QA remains. |
| Increase Child Limit to 12 | Indefinite hiatus (user-directed) | No capacity toggle or implementation work is authorized. The existing native audit remains reference-only: each generation persists six `SPeepRecord`s and the Next Generation scene owns two six-entry candidate arrays. Do not raise the limit constant or pursue the required persistence, Family Tree, and candidate-array detours unless the user reopens this item. |
| Force Successful Pregnancy | B156 source + fully-enabled linked validation complete | Cheat Upgrade 0x136 persists a one-shot in hidden achievement record 0xA8 bit 0x4. It bypasses only the next eligible native `ChanceOfPregnancy` roll; stock partner/gender eligibility, offspring capacity, multiples, naming, family-tree, and birth logic remain intact. The bit clears only when native `CVillager::Impregnate` returns true, remains armed after a capacity failure, survives unrelated Taters-goal writes, and uses the common post-cheat save path. Live purchase, argument-free eligible attempt, full-family, birth, and save/reload QA remains. |
| Next babies Male/Female | B156 source + fully-enabled linked validation complete | Cheat Upgrades 0x137-0x138 use mutually exclusive persisted bits 0x8/0x10. The first native baby spawn receives gender 0/1; native `InitTwin` clones that baby for twins/triplets, so every baby shares the forced gender. Both bits clear only after native `Impregnate` succeeds. Live store, singleton/multiple, save/reload, and post-birth reset QA remains. |
| Next pregnancy Singleton/Twins/Triplets | B156 source + fully-enabled linked validation complete | Cheat Upgrades 0x139-0x13B use mutually exclusive persisted bits 0x20/0x40/0x80. After the stock multiplicity roll, the selected 1/2/3 replaces `CVillager+0x6B1C` and is clamped to the native `EmptyOffspringSlots` result; all stock spawning, achievements, statistics, and Family Tree writes continue. The count bits clear only after native `Impregnate` succeeds. Live 1/2/3-slot boundary and save/reload QA remains. |
| Complete all Achievements cheat | Source + linked build validated | Cheat row 0x12E checks `IsComplete` before calling native `SetComplete` for every currently enabled base/modded prerequisite row, preserving normal coin awards without duplicate payouts. Completing the final prerequisite then awards Achiever Extraordinaire through its normal last-goal observer. |
| Trophy icon for Complete all collections and future cheats | Current rows source + linked build validated | Complete all collections 0x127 and Complete all Achievements 0x12E use the self-contained trophy descriptor. New future cheat rows should alias that descriptor unless given a dedicated asset. |
| Restore F5 debugger selector and native editors | B154 automated + user live confirmed | F5 opens without the prior house-load crash. F4/F5/F6/F7 and Up/Down internal key maps pass all 16 linked layouts; specialized editor edge-case QA remains. |
| Light editor: edit/place/remove sources | Core editor user-confirmed / hardening pending | Native add/delete/save/type-cycle/mouse-drag routes work in game; B154 corrects + and - direction. Persistence/export, cancel/reset, fault handling, and patch-off parity still need narrow QA. |
| Recreate dummied debug tools | Source audit complete; no native route found | `docs/mobile-debug-editor-check.md` records the checked mobile ABIs and desktop objects: no `BehaviorEditor`/`ContentMapEditor` implementation or usable class methods were found. No replacement editor is added without a recovered native contract. |

## Behaviors and variations

| Request group | Status | Notes |
|---|---|---|
| `Needs to sit down` spontaneous plus thinking/resting/phone/texting/scrapbooking variations and all age/gender/relationship gates | Partial / linked validation | `Needs to sit down` is registered all-ages at weight 450 only under Behavior Patches, and all 16 layouts preserve the gate. Variation age/gender/relationship/location sampling and live stock-weight parity remain. |
| `Ironing clothes` and `Mending a button` spontaneous | Shipped / automated-verified | Adult-only helper uses native age-unit minimum `0x118` (displayed age 14+) and registers Ironing at weight 700; linked 16-layout validation confirms patch-on presence and patch-off absence. Live frequency QA remains. |
| `Petting` not spontaneous | B156 source + linked validation complete | Behavior Patches deliberately does not enable autonomous candidate `0x19A`. The randomized Petting label wrapper remains available only when a manual/native Petting route starts. Live long-session absence QA remains. |
| `Checking weight` spontaneous for all ages | B156 source + linked validation complete | Behavior Patches enables native WeighingSelf candidate `0x046` for all ages at weight 450 while retaining the native scale target and plan. Live location/frequency QA remains. |
| Nursing actions: first words, walking, talking, feeding, lullabies, playing, admiring, peek-a-boo, kissing, pictures | B156 source + linked validation complete | Candidate `0x11F` keeps the native nursing-mother/own-carried-baby gate and weight 450, with the complete requested label pool. Live frequency and praise QA remains. |
| Browsing web: buying online (13+), watching/making/posting memes, cat videos, VideoTube, game/social variations | B156 source + linked validation complete | The existing web route receives the requested label pools. Buying online requires raw age `0x104` (displayed 13+); existing teen/social labels retain their native age route. Exact praise-label preservation is installed. Live frequency/praise QA remains. |
| Taking a nap dream variations | B156 source + linked validation complete | The couch-nap route has the complete 30-label dream pool, including every requested destination/topic, without replacing native nap targeting. Live distribution/praise QA remains. |
| Snowy-weather actions | B156 source + linked validation complete | Snow-play eligibility is refreshed only when `Weather.currentType == 5` (Snowing); patch-off keeps the stock candidate. Live weather-transition/frequency QA remains. |
| Bathroom sink actions; jewelry for females 14+ | B156 source + linked validation complete | Direct sink behaviors `0x0A5-0x0A8` clone the full native `0x0A4` candidate before restoring their IDs/weights. The grooming pools are Behavior-Patches gated, and jewelry requires female gender plus raw age `0x118` (displayed 14+). Live sink/variation QA remains. |
| Manual `Play video games` on computer drop | B156 source + focused compile validation complete | Behavior Patches preserves the stock computer hotspot and every email/repair/career/sickness branch. Only the ordinary manual Browsing web result `0x5A` receives a 50/50 native PlayingVideoGame `0x114` alternative; autonomous weights are unchanged. Live drop-frequency QA remains. |
| Mobile-only furniture actions | B158 source + fresh linked overlay/readback complete; live QA pending | Scope is exactly the 63 genuine mobile rows `0x2AA-0x2E8`; all Invisible/custom/VF3 items are excluded. Forty-one original QAMFs are tracked locally and 22 absent maps are explicit. Fresh B158 readback verifies the writable `.vf2beh` control, 34 manual rows across 17 families, 23 autonomous rows with 12 external candidates, stock-first/false-fallthrough behavior, and linked action markers. The final package applies with mobile furniture enabled; live tree/autonomy, Patio, Picnic, birthday, frequency, timer, interruption, placement/gate, save/reload, disable/restore, and crash QA remain player work. |
| Mobile sound assets | B158 source + fresh linked FMOD route/pointer readback complete; audible QA pending | All 67 mobile sound payloads remain hash-pinned, with four WAV-to-OGG replacements. The fresh B158 executable readback resolves `0x01`, `0x35`, `0x39`, and `0x3A` to unique OGG literals and exact Sound records; the pinned FMOD hash is present. In-game FMOD decoding, audible parity, trigger coverage, save/reload behavior, disable/restoration, and crash QA remain player work. |
| Praise string-change bug | B156 source + linked validation complete | InvokeReward captures the exact 0x28-byte action label before native plan clearing, awards exact-label goals, and restores the cached label only for the same behavior serial and incremented praise counter. Scold captures exact text before the single native ForgetPlans call. Live repeated-praise/scold QA remains. |

## Cheats and house state

| Request | Status |
|---|---|
| Spawn max house trash / Spawn max weeds | B156 source updated; relink and live QA pending | Cheat Upgrade `0x12F` delegates to the native trash, dirt-smudge, and sock spawners in the stock 10/10/10 sequence; `0x130` delegates to the native weed spawner with its bounded maximum. The native routines own slot selection and spawn-area routing. Their condensed descriptions warn that the actions will not work while the Maid/Gardener is active. |
| Max out sock pile / No sock pile | B156 source updated; relink and live QA pending | Cheat Upgrade `0x133` now changes only the stock laundry-room sock-pile counter at `theGameState+0x148` to `0x7FFFFFFF`; it no longer spawns loose socks throughout the house. No sock pile uses 0 and deliberately does not award sock-laundering achievement progress. Live purchase/visual/save QA remains. |
| Clean House / Clean Garden | B156 source + fully-enabled linked validation complete | Clean Garden item `0x131` calls the exact stock Weed Bomb selector `RemoveAll(0x7D)`. Clean House item `0x135` matches the stock Housekeeping Services event by calling `RemoveAll` with selectors `0x73`, `0x79`, `0x81`, and `0x83`; yard weeds and the separate laundry-room sock pile remain. Live purchase/remove/save QA remains. |
| Force Marriage Email (Female) / Force Marriage Email (Male) | B158 source/native audit complete; linked/runtime and live QA pending | Adjacent Special Upgrades `0x132` (Female) and `0x14C` (Male) retain the native two-adult crash guard, duplicate suppression, ten-slot queue behavior, and common save path. The scene-scoped `.vf2proposal` byte is `0x00` off, `0x01` female, or `0x02` male; candidate generation changes only the gender value and does not use RNG for either cheat row. Both armed scenes add the ID3 Exit control (image `0x129`, text `0x76C`, widescreen offset +4, y `0x2DD`). Exit plays sound `0x8A`, copies `GameState+0x25CB8` to `+0x25CBC`, clears `+0x25CB8`, and clears the mode; Accept clears the mode before the stock path, while Destroy remains defensive cleanup. Ordinary proposal screens remain unchanged. |
| Divorce Spouse Cheat Upgrade | B158 source/native audit complete; linked/runtime and live QA pending | `Spouse` is always the second-listed adult in the current active generation's Family Tree and targets only `CFamilyTree::GetCurrentFamily()` second parent slot `+0xDC`, requiring presence at `+0xF6` and manager slot at `+0x104` in `0..29`. availability must fail closed unless `VillagerExists(slot, false)` succeeds, that resident's `CVillager+0x1BB48` matches the slot, and live health at `+0x6B00` is positive; no gender, household order, selection, role, or historical-generation heuristic is used. Apply clears exact focus when applicable, calls native `CVillager::DetachAll` and `CVillager::Reset`, zeros exactly `0xD8` bytes at current-family `+0xDC`, calls native `CFamilyTree::UpdateCurrentFamilyRecord`, and uses the common save path. It does not invoke health/death, death flags, or New Generation behavior. historical/retired generations are never iterated. The long description contains exactly: `WARNING: Permanently removes spouse from the Family Tree and House!` |
| Clear All Illnesses | Source + linked build complete; live QA pending | Cheat Upgrade `0x153` cures every symptom and infection for each resident the house currently holds. `CVillagerManager::CureAllVillagers` is a one-byte `ret` stub in this build and is deliberately not used. The cure reproduces exactly the two loops `CVillagerState::Reset` runs -- 7 symptom flags at `+0x5C` with dword expiries at `+0x64`, 4 infection flags at `+0x84` with dword expiries at `+0x88`, and the 16-bit field at `+0x80` between them -- and touches none of the health/age/fertility fields `Reset` goes on to overwrite. `CVillagerState::IsSick` reads precisely those seven symptom flags. Away villagers are skipped via `VillagerExists(index, false)`. Live purchase/cure/save QA remains. |
| Max out Happiness / Energy / Fed / Health Bar | Source + linked build complete; live QA pending | Cheat Upgrades `0x154`-`0x157` call the native `SetHappiness`, `SetEnergy`, `SetHunger`, and `SetHealth` setters, which clamp only the low end, so the ceiling is supplied here as 100 -- the value `CVillagerManager`'s own restore-to-full call passes to `SetHealth`, and the scale `UpdateEnergyState`'s thresholds of 10, 70 and 90 are measured against. Fed is stored inverted: the field counts hunger, proven by every `theMainScene` food drop calling `AdjustHunger` with a negative amount (-2, -3, -4, -7) and by the hunger-90 penalty in `UpdateEnergyState`, so a full Fed bar is the minimum of the range. No `Update*State` call follows, matching the stock food-drop path. All four use the stock Health Plan store icon. Live purchase/bar/save QA remains. |
| Cancel a pregnancy one-shot by repurchasing it | Source + linked build complete; live QA pending | Cheat Upgrades `0x136`-`0x13B` stay purchasable while armed so a repurchase reaches the click handler, and each routes through `VF2ToggleOneShotUpgrade(bit, group)`: buying an armed row clears it, and arming one clears the rows it is mutually exclusive with (male/female share `0x18`, the litter sizes share `0xE0`, Force Successful Pregnancy stands alone). Previously `0x136` reported itself unavailable once armed, so the store drew a checkmark with no way to clear it. Live purchase/cancel/save QA remains. |
| Set/remove Anti-Spam and Rockhound ownership from cheat rows | Source + linked build complete; live QA pending | The drop-on-computer route was abandoned: the stock branch accepts only computer hotspot `0x12`, and a diagnostic build reported the office computer as hotspot 66 with `0x12` absent from a 1600x1200 sweep (the lookup itself was healthy -- 1486 non-zero hotspots), so the refusal and the consumed disk were both stock behaviour reached before any patch code. Flea Market rows `0x33` and `0x10a` are now entirely base game -- stock availability, price, removal and long descriptions, with every shared resolver's reversible-row special case removed. Cheat Upgrades `0x158`/`0x159` toggle the ownership flags instead (`theGameState+0x6C` and native TakeOne/ReturnOne), stay purchasable while active so a repurchase switches them back, and show the store checkmark. Store icons are placeholders: the Flea Market item art lives in the InventoryItems.png atlas with no verified item-to-cell mapping. Live purchase/toggle/save QA remains. |
| Function-sort every Cheat Upgrade | B156 source + fully-enabled linked validation complete; live UI QA pending | Rows are ordered by function: money, food, furniture locks, paired achievement controls, paired collection controls, price multipliers/reset, paired malfunction controls, house trash/Clean House, yard weeds/Clean Garden, sock-pile max/clear, marriage email, forced pregnancy, paired baby gender, then singleton/twins/triplets. Item IDs and effects remain unchanged. |
| Collector Sell All rarity payment and deliberate reset only | Shipped / in-game QA pending |

## Goals and achievements

Exact-action goals trigger only while the named action/variation is current.
Praise/scold must not overwrite it first. Counting goals persist, award once,
and reset only through their documented reset action.

| Group | Status | Requests |
|---|---|---|
| Wealth/food | B156 source + fully-enabled linked validation complete | No More Worries 0x83 awards at the exact native coin ceiling of 4,000,000,000; Solving World Hunger 0x84 awards at the exact food ceiling of 2,147,483,647. All native Set/Adjust callsites are observed, old maxed saves reconcile after load, and Reset Achievements stays cleared until a later resource mutation or reload. Live award/notification/save QA remains. |
| Pets in home | B156 source + fully-enabled linked validation complete | A Furry Companion 0x8A awards for any successfully placed live pet; The Cat's Meow 0x8B is any cat; Man's Best Friend 0x8C is any dog; Slow and Steady 0xA7 is Turtle item 0x245; Itsy Bitsy 0x8D is Tarantula 0x248; Hampster Dance 0x8E is Hamster 0x247; Lovely Lizards 0x8F is Lizard 0x246. Buying into the Tool Tray does not qualify. Failed/full-capacity placement does not award. Successful save loading reconciles only active pets across all 30 native slots. Live placement, notification, reset, save, and reload QA remains. |
| Longevity | B156 source + fully-enabled linked validation complete | Lucky 70's 0x85, Great 80's 0x86, Mighty 90's 0x87, Centenarian 0x88, and Oldest Person in History 0x89 award at exact raw-age thresholds 1400, 1600, 1800, 2000, and 2441 (>122). The annual old-age path observes the raw-age cursor after native food-group calculation but before mortality, independently of the optional mortality flag. Load reconciliation scans the 30 villager records and excludes inactive, left-home, and dead villagers. Live birthday, notification, reset, save, and reload QA remains. |
| Family-tree appearance | B156 source + fully-enabled linked validation complete | Return of the Rainbow 0x90 requires a persistent female (gender 1) family-tree record with head 48; Spiky! 0x91 requires a persistent male (gender 0) record with head 48. All six native record-update calls are observed after the native write. Load reconciliation scans both parents and up to six children across the 30-generation persistent tree, including dead and departed relatives. Live birth/adoption, notification, reset, save, and reload QA remains. |
| LDW paintings/posters | Shipped / automated-verified | IDs 0x60-0x65 award after successful purchases of the six verified LDW art items. Live purchase/notification QA remains. |
| Exact praise: web/games | B156 source + 16-layout linked validation complete; live QA pending | Existing IDs 0x66-0x6A remain exact. IDs 0x98-0xA0 add Sim-ling Rivalry, Blocky Business, Dovahkiin, Reshaping the World, Farming Fanatic, Forum Browser, Explore Collect Compete, Waddle On!, and Pixel Pets. The Poptropicals, Club Puffle, and PetKinz labels were added to the reachable video-game variation table. Near matches do not award. |
| Exact scold: social/child | B156 source + 16-layout linked validation complete; live QA pending | Fakebook Fakery 0x94, Dance Dunce 0x95, The Last Trend 0x96, and child-only Lazy Crazy 0x97 inspect the exact pre-clear action label. The reachable native label is `Posting on Picstagram`; the requested achievement text retains `Clipstagram`. |
| Pet interaction corrections | B156 source + 16-layout linked validation complete; live QA pending | Good boy 0x6B and Bad dog 0x6C inspect exact `Praising pet`/`Scolding pet` labels before native state clearing. Pavlovian Association 0x93 now awards only when the player praises a villager whose exact current action is `Training pet`. It is visible only with Behavior Patches; Achiever Extraordinaire remains final. Their descriptions were corrected in the in-place B174 refresh (2026-08-26) to match this implementation: both read "You praised/scolded someone who was praising/scolding their pet", because the trigger inspects the PERSON's action label, and the previous "You praised a pet" described an action the game does not have. Live praise, popup, persistence, reset, and patch-off QA remains. |
| Birthday purchases | B156 source + 16-layout linked validation complete | Happy Birthday 0x80 maps Birthday Banner 0x2DB; Not a lie 0x81 maps Birthday Cake 0x2DC; Full of helium 0x82 maps Birthday Balloons 0x2DA. Awards occur only after AddToStorage succeeds; live purchase/save/notification QA remains. |
| Discipline | B156 source + 16-layout linked validation complete; live QA pending | Child-only exact scolds award No clothes-throwing 0xA1, No playing in the toilet 0xA2, No drawing on the wall 0xA3, and No messing with the light switch 0xA4. The stock game already supplies No jumping on the bed. Props to you 0xA5 requires stock Tight Ship 0x30—which itself proves all three stock discipline goals including bed-jumping—plus all four new goals; qualifying saves reconcile on load. |
| Holiday Furniture purchases | Shipped / core purchase goals user-confirmed | Coin awards and purchase-goal firing work in game; exhaustive ID aliases, persistence, and patch-off absence remain narrow QA. |
| VF3 furniture | B156 source + 16-layout linked validation complete; live QA pending | Furnishing the Future 0xA6 awards after a successful purchase of any active VF3 furniture-patch item: six couches/loveseats 0x2F6-0x2FB or three televisions 0x324-0x326. Failed storage purchases do not award. |
| Ornamentologist/collection goals | Shipped / spawn and award user-confirmed | Ornaments spawn and Ornamentologist wiring works in game; exact Lucky Rock weighting is source/link proven. Persistence, live frequency, and every reset path remain narrow QA. |
| Achiever Extraordinaire | B156 source + fully-enabled linked validation complete | ID `0x92` is always the final visible row. Every completion scans the exact selected `achievementOrder`, excluding only itself; compile-gated Ornament/Behavior goals and runtime-gated Holiday Furniture goals count only when visible. Successful save load performs the same reconciliation. Live final-award/popup/save QA remains. |

## Renovations, map, events, and family systems

| Request | Status | Completion contract |
|---|---|---|
| Mobile Kitchen/Office/Workshop/first Bathroom renovations | B158 direct-state fix + linked validation complete; live QA pending | Fifteen upright, hash-verified mobile room PNGs are local under `work/assets/mobile_renovations/`. The optional toggle registers PC styles `0x13C-0x14A` only in native House Renovations category `0x11` (`gHomeList`/`gHomeListSorted`), expanding that list from 10 to 25; they are absent from Special Upgrades. Added rows are grouped Bathroom, Kitchen, Office, Workshop without changing their item IDs or native mobile bindings. The renderer draws the complete images 1:1 at stitched-map-calibrated Bathroom `(535,1263)`, unchanged Kitchen `(930,995)`, Office `(1357,792)`, and Workshop `(900,1475)` anchors from the post-map `theMainScene::DrawScene +0x39` hook. Activation reads and writes the proven persisted byte at `InventoryManager + itemId + 0x2A3` directly because stock `TakeOne`/`ReturnOne` reject these image-bearing records through their metadata gate. Room switching, duplicate normalization, direct removal, legacy ever-purchased backfill, inactive re-buy catalog pricing, and immediate saves preserve the separate purchase-history mask. Default-off builds retain the stock map path. Live purchase, switching/removal, save/load, camera, and patch-off visual QA remain. |
| Same remodels for second Bathroom | B158 separate PC-only visual route; live QA pending | Bathroom 2 PC House Renovation rows `0x14D-0x151` use the supplied per-color overlays and closed-curtain selector. Blue `0x14E` selects the supplied `curtain_closed_blue.png` payload; inactive or unavailable custom descriptors fall back to stock `Images/curtain_closed.png`. The native `0xE6` second-bathroom route remains untouched and is not implied by this visual route. |
| Every active-scope renovation reversible | B158 source implementation; live QA pending | Selecting an owned native structural renovation `0xE1-0xEA` in the store returns that upgrade, reloads the base `CContentMap`, reapplies every remaining native renovation record from the exact `theGameState::Load` table, and saves. Mobile cosmetic styles `0x13C-0x14A` use one active style per room plus persistent purchase history: buying the active style again removes it, and a previously purchased inactive style is free to reactivate. Their descriptions say `Buy again to remove.` Second Bathroom is excluded while on indefinite hiatus. Live remove/rebuy, switching, save/load, and visual QA remain. |
| Expanded map X-1Y0..3 and X4Y0..3 | Removed from B156 scope by user request | No expanded-map patch is exposed. The inactive placeholder and Experimental/Not Working section were removed; no camera, pathing, placement, spawn, or save bounds are changed. |
| VF3 Phone | Indefinite hiatus (user-directed) | No implementation or research is authorized. The existing email system remains unchanged unless the user reopens this item. |
| Mobile Island Events and exact outcomes | B158 enabled build + linked validation complete; live QA remains | The enabled overlay appends all 25 authenticated mobile event records/text routes at slots `0x61-0x79`; 23 use recovered firing/award/impact routes and 2 preserve unconditional mobile `CanFire=false`. Coin outcomes call native `CMoney::Adjust`; furniture outcomes call native `FurnitureManager.AddToStorage`; tool outcomes call native `ToolTray.AddItem`; fossil outcomes call native `CCollectableItem::Add`. Live dialog, targeting, choices/effects, persistence, nonappearance, and patch-off QA remains. |
| Enable Races via VF3 palette/overlay | Indefinite hiatus (user-directed) | No implementation or research is authorized unless the user reopens this item. |
| Same-sex marriage | B158 source implementation; linked static validation complete; live QA remains | The default-off `.vf2same` support flips only `CVillager+0x6A58` on the spawned marriage candidate after native `GetVillager` returns; the native opposite-sex spawn argument, proposal queue, Accept, Reject, close, proposal state, parent storage, and candidate selectors remain unchanged. An established enabled same-sex spouse pair uses the native private-romantic-time sequence, `TryToMakeBaby` returns before pregnancy (0%), and that pair does not take refusal or argument outcomes. Live marriage email, acceptance, private time, save/load, and patch-off QA remains. |
| Multiple candidates per marriage email | B156 source + 16-layout linked validation complete; live QA remains | Reject keeps the active proposal scene open and calls the native candidate generator, which deactivates the rejected temporary villager before displaying one replacement. The stock proposal timestamp/email fields are not cleared. Flag-off replacements remain opposite-sex; `.vf2same` replacements independently choose either gender; a cheat-triggered proposal inherits its one-scene `.vf2proposal` random-gender mode. Accept remains the untouched native route. |
| VF3-style child adoption chooser | REVERTED in B171; Adoption Services is base-game | Purchasing Adoption Services crashed: access violation, faulting module `unknown`, fault offset `0x1D7` -- execution outside every loaded module, the signature of a corrupted return address or function pointer. The chooser replaced the stock spawn route at `CScrollingStoreScene::HandleUpgrade+0x57A`; two things in it were wrong against the native code and either could have been fatal -- it called `SpawnSpecificPeep(age, gender, body=-1)` where the native route passes `(1, -1, 0x3C)`, and it constructed a `theMessageBoxDlg` in a `0x300`-byte stack buffer standing in for a class whose real size is not pinned anywhere. Rather than guess which, the whole route was reverted. `HandleUpgrade` is byte-identical to the stock object (diffed against `work/desktop_obj_files`: no changed bytes; `+0x57A` back to `6a 3c 6a ff 6a`). No baby/older-child prompt exists in any B171 executable; do not test or claim it. Reopening this needs the message-box class size established first. |
| Generation counter beyond 30 | B156 source + 16-layout linked validation complete | Every successful native `StartNextGeneration` call increments a separate 24-bit lifetime counter persisted in hidden achievement record `0xA8` bits 8-31. Existing saves with no counter seed from the current stock generation; failed starts do not increment. The stock 30-record `MakeRoomInTree` rollover is unchanged. The Goals screen draws `Generation: N`, and Reset Achievements preserves the counter while a true new-game achievement reset still clears it. Live generation transition, save/reload, Goals placement, generation 30-to-31 rollover, and new-game reset QA remains. |

## UI, packaging, and documentation

| Request | Status | Notes |
|---|---|---|
| Vanilla-save compatibility note | Shipped / automated-verified | Exact requested sentence is shared by exporter and GUI and covered by tests. |
| Creator/passion-project/support message | Shipped / automated-verified | Exact requested copy is shared by exporter and GUI and covered by tests. |
| Brokerage rate up to 11% message | Shipped / in-game QA pending | Exact native description is present; visible in-game wrapping remains. |
| Smaller builds / remove redundant EXEs | Partial | B152 is manifest-pruned; 16 native overlays remain required by current architecture. Prefer exact-SHA toggles when safe. |
| Preserve base-game EXE icon | Implemented / automated Windows verification | B156 copies exact stock RT_ICON and RT_GROUP_ICON resources after all EXE mutations, validates group references/sizes, and requires successful Windows extraction at 16x16, 32x32, and 48x48. Player confirmation of folder/desktop/taskbar display remains final live QA. |
| Designated GitHub repository only | Shipped / automated-verified | B152 branch/release is in Lorsieab2/Virtual-Families-2-Restoration-Addition-Patcher. |
| Complete changelog and Transparency Log | Ongoing invariant | Required before every release. |
| GitHub publishing/latest release asset only | Ongoing invariant | Commit/push source, verify latest ZIP digest, delete obsolete local release archives. |
| RestingBody autonomous under Behavior Patches | Shipped / in-game QA pending | B175 restores 0x127 to VF2EnableAutonomousCandidates as an all-ages candidate at weight 450, before the VF2EnableMobileFurnitureCandidates call that raises it to 2000 when .vf2beh is set. PR #60 had removed it, leaving the behavior and its resting labels unselectable whenever Mobile Furniture Behaviors was off. Verified in the linked binary (two candidate writes, 450 then 2000). No villager has been observed choosing it in game. |
| All-enabled playtest actually enables every runtime gate | Shipped / in-game QA pending | B174.2 and B174.3 shipped with .vf2goal, .vf2preg, .vf2mort and .vf2scrl still 00 despite the artifact name, so Holiday Furniture goals, Allow Older Pregnancies, the Older Villager Mortality curve and the Store Scroll Bar were present but dormant. build_playtest.ps1 now drives all five from enable_runtime_flag.py and prints each. B175 reads 01 for all five. Note this makes two default-off experimental options on by default in a playtest artifact. |
| Reproducible build from source | Yes | The historical inventory records 635 images that used to reach a build only by inheriting from a previous build output. 95 of them are editing sources (30 .xcf, the 61 Images/Upgrades "invisible images" and "original images" working files, and four named backup copies with no runtime reference) that the engine never loads and the offline bundle excludes, leaving 540 runtime images. All are tracked under patcher_assets/inherited_runtime_images with recorded digests, and the build now consumes them: restore_preserved_inherited_art() supplies what the generators did not produce, verifies every file it writes against its digest, and stops the build on a missing store, a missing digest, or a mismatch. An unseeded build produces 540/540 runtime images (92 restored, 448 rebuilt by the holiday generator from the tracked source archive) and writes no editing sources. A seeded build produces byte-identical inherited art (540/540 match the unseeded build), because seed-carried bytes are verified against the tracked digest and replaced when they differ, while anything a generator rewrote is left authoritative; the editing sources a seed drags in are removed. build_playtest.ps1 validates the same 540, and no longer refuses a partial seed now that the store can supply what it lacks. |
| Ping-Pong Table store item | Shipped / in-game QA pending | Item 0x32E, gFurniture5, price 12000 (the Pool Table's own), Furniture/Placeable, borrowing PoolTableStd's fmap and behaviour. PR #110 unregistered it by reading "drop the ping pong table" as dropping the whole item; only the recruit-two-people part was meant to go. Restored in PR #119. There was nothing to remove: CBehavior::PlayingPooltable is single-villager end to end (one PlanToGo to EObject 0x36, StandNW/NE anim, sound 0x112, 50/50 cheer-or-sigh) and the donor fmap is a plain 139-solid/67-walkable footprint with no seat anchors, so no two-villager recruitment exists in it. |
| "Playing ping-pong" villager action | Source complete / in-game QA pending | The Ping-Pong Table borrows CBehavior::PlayingPooltable from the Pool Table, which is what gives it any interaction at all, but that behaviour labels its users "Playing pool". One label is added -- "Playing ping-pong" -- in a ping_pong group appended after the established groups so none of their ids shift. ("Rallying back and forth" shipped in B179 and was removed afterwards on request; the exercise bike groups were appended after ping_pong later, so it is no longer the last group.) Both tables answer to EObject 0x36, so the wrapper asks the question the native behaviour itself asks: FeetPos() then FindFurniture(0x36, feet, info, true, 0, 0), read-only and nearest-match, then PtOnFurniture to resolve that point to a placed slot and the item id from record+0x00 (count +0x1004, stride 0x40, in-world flag record+0x0C). An earlier version called LinkPeepToFurniture instead, which MUTATES by reserving a link and answers "which table can this villager be given" rather than "which table is it at" -- that is why the label stayed "Playing pool" in B179. record+0x10 is an orientation index, not a point. The probe takes the object as a parameter because it differs per behaviour: the treadmill behaviours search 0x04, and hardcoding 0x36 made the exercise bike probe never match. A stock Pool Table returns early with its stock label untouched and the native plan is unchanged. Live QA remains: confirm the label appears on a placed Ping-Pong Table and that pool tables are unaffected. |
| Hairstyle rows in Clothing and Hairstyles | Source complete / in-game QA pending | 100 rows (50 head values x 2 genders) at 0x480-0x4F1, riding the outfit tool because the game has no hairstyle tool. PR #106 landed the mechanism inert -- gVF2LastSyntheticHeadByGender was read twice and written never, so VF2ApplyPendingHairstyle could only return -1 -- and PR #111 deleted it rather than finishing it. PR #120 restores and completes it: the missing write (VF2SetSelectedHeadForSyntheticItem, called from the purchase route and tray normalisation), gClothingList registration, icons cut from the game's own head sheets at frame 5 of each 24-frame 28x56 row, appended image and string ids, and price/icon/description/lock accessors that answer for head ids. Selecting a hairstyle clears any pending outfit and vice versa, since both use the one tool. The B96 final-apply callsite replacement remains uninstalled. |
| gClothingList bounds guard past 128 rows | Fixed in source / in-game QA pending | GetCategoryItem guards the clothing category with cmp esi,5 / ja at +0x313, previously patched in place as "83 FE ib". That imm8 is sign-extended while the JA is unsigned, so any max index above 127 inverts the guard -- 213 becomes 0xD5, compares as 0xFFFFFFD5, and the store would read past the end of the list. Latent at today's 114 outfit rows (max index 113); reached by the 100 hairstyle rows. PR #120 emits the 6-byte "81 FE id" form instead, which needs PR #107's code-section branch fix-up because it is three bytes longer. |

| Added furniture actually does something on a drop | Source complete / in-game QA pending | The owner reported the added furniture "does nothing when villagers are supposed to interact with them". theMainScene::VF2HandleDropOnMobileFurniture dispatches on ITEM ID and, of the twelve added items, only the Invisible Spa Lounger (0x32F) had a route, so the rest placed correctly and then had nothing asking them to act -- which is why the Spa Lounger appeared to be the only one working. Routes added for the Invisible Picnic Table (0x328), Invisible Patio Table (0x329), Invisible Lounger (0x32B, folded into VF2IsMobileChaise so it inherits every chaise behaviour rather than a copy of one) and the new visible Spa Lounger (0x330). Ids come from furniture_item_id_by_name() against NEW_FURNITURE_ITEMS and INVISIBLE_OUTDOOR_ITEMS; no route, binding or test pins a bare literal. Route coverage is checked by parsing the EMITTED vf2_mobile_furniture_behaviors.cpp, because routes are written as __VF2_*__ placeholders that only resolve at emit time. |
| Stock-donor added furniture (pools, hammock, yoga, bike, home gym, ping-pong) | Needs player confirmation | Seven items are deliberately NOT routed through the mobile dispatcher: Invisible Kiddie Pool (0x30A), Invisible Full-Size Pool (0x30B), Invisible Hammock (0x30C), Invisible Yoga Equipment (0x32A), Exercise Bike (0x32C), Home Gym System (0x32D) and Ping-Pong Table (0x32E). They borrow STOCK desktop furniture, whose drop handling runs through theMainScene::HandleDropOnHotSpot, which executes BEFORE the mobile dispatcher and dispatches on hotspot rather than item id. Their .fmap cell vocabularies were compared against the donor maps and are byte-identical, so the hotspot data they carry is already the donor data. Evidence the native path does reach them: the Ping-Pong Table demonstrably ran PlayingPooltable, which is why it displayed "playing pool". Building handlers for these would duplicate a path that already runs. OUTSTANDING: the owner should drop a villager on the Exercise Bike and the Invisible Yoga Equipment and report whether anything happens. If they act, this closes; if not, handlers are built from the donors' stock behaviours. The bundled player documentation has been corrected to match: the B180 changelog line, which reaches players through the synthesized README-*-PATCHER.txt and the bundled transparency log, previously described all seven as merely awaiting player confirmation. It now says plainly that the Home Gym System does not work and its behaviour is not implemented, while keeping the Exercise Bike and Invisible Yoga Equipment named as unconfirmed, since those are different states. Note also that the Home Gym does NOT inherit its donor's price: the Yoga Equipment donor is 150 and the gym is 3150, configured independently, so the donor supplies the furniture map, store slot and behaviour arrangement but not the price. Not claimed as working in any shipped doc. |
| Exercise Bike action labels | Source complete / in-game QA pending | The Exercise Bike borrows the Treadmill's two behaviours and was labelled "walking on the treadmill" and "running on the treadmill". Two label groups are appended after ping_pong so no established id shifts: eString_UsingTheExerciseBike ("Using the exercise bike") retargets WorkoutTreadmill 0x049, and eString_DoingHighIntensityCycling ("Doing high-intensity cycling") retargets RunningOnTreadmill 0x0E0. Animations are unchanged at the owner's explicit instruction ("As for the animations...nah don't change them"). The probe searches object 0x04, not the pool table's 0x36; it had been hardcoded to 0x36, which would have made the bike labels silently never apply. Confirmed against Behavior_patched_disasm.txt. A stock Treadmill keeps its stock labels. |
| Store rows show a checkmark when active | Source complete / in-game QA pending | The store draws a checkmark on any row whose GetNumAvailable is zero. Rows whose "active" is a reversible state rather than a purchase are answered from live game state via VF2StoreRowShowsActive at the DRAW site only: the four Special Upgrades (Brokerage Account against Money.bankingInterest, Food Club and Lucky Rock against their own flags, Health Plan against the saved entitlement), Unlock Everything In The Store, the three price multipliers and Reset Price Multiplier (mutually exclusive, so Reset ticks when none is in force), Enable Same-Sex Marriage and Allow Reroll Of Marriage Candidates, the pregnancy cheat mask rows, Anti-Spam and Rockhound ownership, the ten native house renovations 0xE1-0xEA, and both added renovation catalogues. The click path is left reading the real answer so reversible rows stay clickable -- rebuying Unlock Everything restores the locks and rebuying a multiplier still switches to it. |
| Hairstyle store icons cut off | Fixed in source / in-game QA pending | The owner reported the icons were sliced in half and should look like the 5th column of each row. They were being cut on the engine's own indexing cell of 28x56, but the drawn heads are about 31px wide and centred on 12 visual frames of 56x56 within the 672x2800 sheet, so a 28px cut removed half of every head. Confirmed by locating the fully transparent gutters in female_heads00.png at columns 45-69, 101-121 and 153-178 -- a 56px rhythm. Icons are now cut at HEAD_STORE_ICON_FRAME 5 of HEAD_STORE_ICON_FRAME_COUNT 12 on a 56x56 cell. The 28x56 cell is retained as HEAD_STORE_CELL_SIZE and still consistency-checked, because it remains correct for indexing. |
| Spa Lounger (visible) and autonomous spa treatment | Source complete / in-game QA pending | A visible Spa Lounger (0x330, donor 0x26F, gFurniture5, price 250) was added alongside the invisible one (0x32F) and shares one route, so both behave identically. Art is SpaLoungerStd.png, 198x79, two frames of 99x79, supplied by the owner and registered in patcher_assets/new_furniture_art/SHA256SUMS.json. Per "once you wire in the manual drop behaviors, make them autonomous too" and "yes make the receiving half autonomous", the RECEIVING half is an autonomous candidate; the giving half is not, because giving needs a second villager already receiving on that same lounger and autonomous selection picks one villager at a time, so an autonomous giver would mime a massage at an empty chair. The candidate resolves an actual free spa lounger from the placed furniture array before committing: gating on eObjectChaise alone is insufficient because both spa loungers share that object with every stock and mobile lounger, and checking occupancy at the villager's current position cannot help because it samples a position chosen before any destination exists. Duration, posture and the gulp-and-sigh (eSound_GulpAhh_01, 0x101) come from the game's own nap and refreshing-drink behaviours; posture follows the placed lounger's orientation so a villager does not lie across the arm of a lounger placed the other way round. |
| Patcher "Please wait" popup | Shipped / player-visible, GUI QA pending | The owner asked for the Virtual Villagers patcher's wait window to be brought into this patcher, trading our Check-for-updates link and version display back to that project. Shipped in B180: a modal window reading "Please wait... / Loading the patches." over an indeterminate progress bar, with the blocking work moved onto a worker thread so the Tk event loop keeps painting and Windows stops relabelling the window "(Not Responding)". Two defects were found in the port and fixed here -- a modal grab swallowed inside a bare `except`, which left every control underneath live so a double-click on Apply could start two patch workers against one output folder, and a centring clamp that threw the window onto the primary monitor on a multi-monitor desktop. `wait_visibility()` is deliberately NOT the repair for the grab: it blocks forever on a withdrawn parent. The return half of the trade was already satisfied -- that project already carries a version display and a releases link, and had already fixed the clamp -- so the grab fix was sent back instead, as Virtual-Villagers-Fun-Patcher#221. Live GUI QA on a multi-monitor setup and on a slow copy remains player work. |
| Mobile furniture fmap inheritance | Shipped / in-game QA pending | The owner reported villagers sitting wrongly at the Patio Table's chairs and asked that mobile furniture inherit its maps from the mobile version. Two distinct defects, both shipped fixed in B180. First, `Patio_table.png.fmap` had its object-type field stripped by 0x1b0 -- the chaise's type, copied from the neighbouring spec -- instead of its own 0x1b4, leaving both seat anchors claiming furniture type 0x4; it was the only one of the 34 maps with a partial strip. Second, and separately, an item BORROWING a donor that Mobile Furniture Behaviors implements was written the donor's raw mobile map rather than the desktop-safe one, because the patch installs desktop-safe maps only over the 34 names it owns and a borrower's copy is written under the borrower's name. That left both Spa Loungers with the untranslated mobile peep-slot anchor 0x01B09800 instead of 0x00009800, which the behavior ledger records as making desktop `FindPeepSlot` reject every chair -- the cause of the owner's NW/NE lying-position report. Exactly four generated maps change; verified by building main and the fix from one seed and diffing all 327 maps in the output. Live QA on seat positions at both tables and on both Spa Loungers remains player work. |
| Picnic and patio table props | Art supplied / rendering pending | The owner asked that all added furniture carry the props it needs, naming the Picnic Table (meal sprites) and Patio Table (drinks sprites). They are NOT shipped, and this is a hard engine limit rather than an oversight. CEnvironment::SetProp opens with cmp edi,54h / ja, so the desktop build accepts prop ids 0x00-0x54 only. ePropPicnicReady is 0x55 and ePropPatioDrinks is 0x56, both out of range, and the fall-through path writes through [ecx + prop*16 + 0x7C] -- passing either would corrupt memory rather than fail cleanly. The existing external-state approach was avoiding a crash, not overlooking the props. Re-examined for B180: the bound cannot simply be raised. The compare at Environment.obj+0xAB33 is followed by `0F B6 87 <reloc>` / `FF 24 85 <reloc>` -- a compiler-generated jump table whose byte-index and dword-target displacements are both link-time relocations. Admitting 0x55/0x56 would read past the end of the index table and jump through whatever followed. Doing it properly means extending both tables and writing case bodies, which is a feature rather than a bound adjustment. Correcting one thing this row previously claimed: it said the props have no desktop art. The base game does ship `Images/meal.png` (160x68), and it is present in the runtime payload, so meal art exists and is not the obstacle. What is missing is a desktop *handler* -- a jump table case that draws it -- and all 27 existing SetProp cases are claimed, with every jump entry a DIR32 relocation. So the blocker is the dispatch table, not the artwork, and the row should not be read as 'the artwork does not exist'. UPDATE: the owner has since supplied the art -- mealSE.png (105x71), mealSW.png (115x67) and patioDrinks.png (44x39), all RGBA, checked in under prop_art/ and hash-pinned. The artwork half is therefore closed. What remains is drawing: the ids never reach the engine, because VF2PatioSetPropAndTrack intercepts 0x55/0x56 before SetProp sees them and tracks the state externally, so a draw hook has to render these sprites itself. That is the same detour pattern already used for DrawScene and DrawVillager, and it needs no change to Environment.obj -- which makes it the safe route the owner asked for, rather than extending the jump table. STATUS: the artwork question is closed and the engine question is answered; what remains is implementation. The owner supplied the sprites, and a second session independently decoded the same dimensions out of the mobile OBB's tp7.dat (mealse 105x71 and mealsw 115x67 both exact), so the art is right. The draw hook is the outstanding work, and it is ordinary implementation rather than a blocker: the same detour pattern already proven on DrawScene and DrawVillager, using the prop surface the object exposes. This row is no longer waiting on a decision. ART NOW SHIPS: the sprites were tracked and hash-pinned but nothing wrote them into a build, so a wrapper would have drawn correctly in development and shown nothing on a real install -- the exporter builds a bundle by diffing the build output, so tracked-but-unemitted art never reaches a player. install_prop_art now writes all three into Images/, digest-verified, following the same pattern as install_new_furniture_art whose SpaLoungerStd.png does reach players. A test fails if a sprite is tracked but left out of the install map. |
| Invisible Spa Lounger sprite | Shipped / in-game QA pending | The owner's rule: "Invisible furniture has visible patches with the Visible Furniture Sprites patch. THAT'S what I want you to install the spalounger.png for regarding the invisible Spa Lounger item. When the transparent graphics patch is enabled, the patcher should swap ONLY the invisible furniture's sprites with a fully-transparent version. For the Normal Spa Lounger, I want it to have the same spalounger.png sprite. This one should not become transparent under any circumstances." So "invisible" names which item the transparency patch may blank; it is not an absence of artwork, and both items ship the SAME sprite. The Invisible Spa Lounger was shipping Chaise_brown.png, so a player who bought it and enabled Visible Graphics saw a generic brown chaise rather than the lounger they purchased. Two changes: the item now sources SpaLoungerStd.png, and sync_invisible_outdoor_sprites now searches patcher_assets/new_furniture_art as well as inherited_runtime_images. That second half was the real defect -- changing the item alone left the build emitting the chaise, because the sync searched only the inherited store and a stale seeded copy satisfied it with no missing-source error. Verified in the BUILD OUTPUT, not the source: both loungers ship SpaLoungerStd.png byte-identically (sha c3606195c1, 198x79), the invisible one has a fully-transparent .pngORIGINAL to swap in (every alpha 0), and the normal one has NO ORIGINAL at all, so it cannot be blanked under any setting. The swap is keyed by item name rather than filename, which matters because both share one sprite file -- keying on the filename would blank the normal lounger too. Live QA on both items with Visible Graphics and with Transparent Graphics remains player work. |
| Widowed adult cannot remarry once children exist | Working as intended / do not fix | Raised as a possible defect and investigated; a fix was built and then withdrawn unmerged. The owner ruled it intentional base-game behaviour: "the widowed adult being unable to remarry naturally if there's already children on the family tree is intentional base-game behavior, regardless if same sex or hetero. do not fix that!" The branch is left in place rather than deleted. Recorded here so the same finding is not re-opened as a bug by a later audit. |
| "Rallying back and forth" label | Removed on request | Shipped in B179 as a second ping-pong label and removed afterwards at the owner's request ("do me a favor and remove this label pls"). Verified absent from both the patch source and every emitted .cpp. The ping_pong group now carries one label. |
| Home Gym System and Yoga Equipment behaviours | Requested / not built | The owner reported the Home Gym System does nothing in play, and then asked for the Yoga Equipment to have behaviours too: "I want behaviors for the yoga equipment too." Both are recorded here rather than folded into one row, because the Home Gym borrows the Yoga Equipment as its donor and a change to one is not automatically a change to the other. WHY THE GYM IS INERT, read out of Behavior.obj rather than inferred: the yoga behaviours are pure in-place animations. DoingKungFu (0x13, SECTB0), DoingTaiChi (0x69, SECTB4), WorkingOut (0x4A, SECT336) and QuickWorkout (0x8B, SECT206) contain no call to FindFurniture, LinkPeepToFurniture or PtOnFurniture at all, so a villager performing them does so wherever they are standing and never consults a placed item. The Yoga Equipment's own map carries object 0x75, and no stock fmap other than YogaGearStd.png.fmap carries it, and no behaviour searches it. So cloning that donor produced a faithful clone of a decoration: the fault is in what was cloned, not in the cloning. THE ONE WORKING PRECEDENT: Aerobics (0x03, SECT1C) is the single gym-adjacent behaviour that DOES consult furniture. It calls FeetPos, then FindFurniture with object 0x0D, checks the returned info.object equals 0x0D and returns if not, sets its label, and walks to info.point. Object 0x0D belongs to OldStyleTV.png.fmap and TVFlatScreenStd.png.fmap -- aerobics is done in front of a television, which is why it looks up furniture when the others do not. That sequence is the template for making any of these behaviours happen AT an item. THE REQUESTED SEMANTICS, stated by the owner twice after an earlier attempt got them backwards: "IF THE GYM IS OWNED IT SHOULD NEVER GATE BEHAVIORS" and "OWNING THE GYM JUST MAKES THEM DO THOSE BEHAVIORS AT THE GYM". The effect of owning the item is positional, never permissive -- the six behaviours stay globally available and ungated, and a placed item is where they happen. An earlier attempt repointed the Home Gym at the Treadmill's fmap so villagers would link to it like a treadmill; that changed what the item IS and was reverted in full at the owner's instruction ("the treadmill approach isn't it"). No version of this feature may gate a behaviour on owning the item. STATUS: mechanism identified and the template located; implementation not started, and not to be started without the owner's explicit go-ahead. |

## Mandatory uncertainty audit

Before each release:

1. Reconcile chat requests against this ledger, TODO, build history, source
   settings/hooks, tests, release notes, and the actual exported payload.
2. Trace every behavior variation's base action, exact text, age/gender/nursing/
   relationship/weather gate, autonomous/manual eligibility, and off-state.
3. Trace every custom goal's trigger, counter, coin award, persistence, reset,
   order, and optional-patch visibility.
4. Trace every reversible purchase's active flag, removal, re-buy, and save/load.
5. Test Holiday Lucky Rock behavior and full Ornament save/load/UI paths.
6. Audit mobile furniture actions, mobile Island Event outcomes, and the
   remaining in-game Brokerage text layout.
7. Any older request found but absent here is added as **Needs source audit**
   before implementation or release; it is never silently omitted.
